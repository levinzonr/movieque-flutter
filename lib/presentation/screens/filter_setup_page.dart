import 'package:flutter/material.dart';

class FilterSetupPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(title: Text("Filters"),),
        body: Placeholder(color: Colors.red,),
      ),
    );
  }
}
